<% layout('/layout/boilerplate') -%>
<div>
    <h3> Edit your list</h3>
    <div class="row">
        <div class="col-8 offset-2">
    <form action="/listing/<%= list._id %>?_method=PUT" method="POST">
        <input type="hidden" name="_method" value="PUT">

        <!-- Hidden input to simulate PUT method -->
         <div>
            <label for="Title" class="form-lable">  Title</label>
            <input type="text" value="<%= list.title %>" name="listing[title]" class="form-control" required>

         </div>
         <div>
            <label for="Description" class="form-lable"> Description</label>
<textarea type="text" value="<%= list.description %>" name="listing[description]"  class="form-control" ></textarea>
         </div>
         <div>
            <label for="Image" class="form-lable"> Image</label>
            <input type="text" value="<%= list.image %>" class="form-control name="listing[image]">

         </div> <div>
            <label for="price" class="form-lable">price</label>
            <input type="number" value="<%= list.price %>" name="listing[price]" class="form-control "required>

         </div> <div>
            <label for=" country" class="form-lable">country</label>
            <input type="text" value="<%= list.country %>" name="listing[country]" required>

         </div> <div>
            <label for="location" class="form-lable">location</label>
            <input type="text" value="<%= list.location %>" name="listing[location]" required>

         </div>
      
        <button class="btn btn-dark add-btn">Submit</button>
    </form>
</div>
</div>
</div>